APP_NAME="Certify LMS"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Docker Compose プロジェクト名 (親ディレクトリが日本語のため明示)
COMPOSE_PROJECT_NAME=certify-lms

# Sail ports (Laravel/Sail 慣習に揃える: web=8000, pma=8080, db=3306, mailpit=8025)
# VITE のみ BookShelf=5173 と衝突するため 5174 に逃がす
APP_PORT=8000
VITE_PORT=5174

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=certify_lms
DB_USERNAME=sail
DB_PASSWORD=password

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# CORS (Sanctum Cookie 認証で別オリジン FE を許可する場合は具体的なオリジンをカンマ区切りで指定)
CORS_ALLOWED_ORIGINS=http://localhost:8000

# Google Calendar 連携(コーチの任意連携、面談予約)
# Google Cloud Console で OAuth クライアント(ウェブアプリケーション)を作成し、
# 承認済みのリダイレクト URI に GOOGLE_REDIRECT_URI と同じ値を登録すること。
# 未設定の場合、連携セクションの UI は表示されるが接続は失敗する(面談機能自体は動作継続)。
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI="${APP_URL}/settings/google-calendar/callback"

# AI 相談(Gemini チャットボット)
# AI_CHAT_ENABLED=false の間は /ai-chat のルート自体が登録されず、画面・ウィジェットとも非表示。
# GEMINI_API_KEY は Google AI Studio (https://aistudio.google.com/apikey) で発行する。
# 未設定でも機能自体は動作継続する(AI 応答のみ常にエラー表示になる)。
AI_CHAT_ENABLED=false
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
